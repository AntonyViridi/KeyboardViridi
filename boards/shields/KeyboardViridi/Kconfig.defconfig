if SHIELD_KeyboardViridi

    config ZMK_KEYBOARD_NAME
        default "ViridiKeyboard"

    config ZMK_USB
	    default y

config BT_MAX_CONN
        default 6

    config BT_MAX_PAIRED
        default 6

endif
